<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <link rel="stylesheet" href="/stylesheets/style.css" />
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
      <a class="text-blue-500" href="/">Home</a>
      <h1 class="text-3xl text-white mb-6">All Users</h1>
      <% if (users.length > 0) { %> <% users.forEach(function(user) { %>
      <div class="users bg-zinc-800 rounded-lg shadow-lg p-6 mb-6">
        <h3 class="text-xl font-bold mb-2"><%= user.name %></h3>
        <h4 class="text-lg text-gray-400 mb-4"><%= user.task %></h4>
        <div class="flex justify-between">
          <a href="/edit/<%= user.id %>" class="text-green-400">Edit User</a>
          <a href="/delete/<%= user._id %>" class="text-red-500">Delete User</a>
        </div>
      </div>
      <% }) %> <% } else { %>
      <p>No users found.</p>
      <% } %>
    </div>
  </body>
</html>
